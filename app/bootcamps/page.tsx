import React from 'react';
import BootcampHero from './(components)/Hero';
import BootcampModes from './(components)/Modes';

const Bootcamp = () => {
  return (
    <div>
      <BootcampHero />
      <BootcampModes />
    </div>
  );
};

export default Bootcamp;
